# Changelog

Alle Änderungen an diesem Projekt werden hier dokumentiert.

## [0.1.0] – 2025-12-01
### Added
- Initial project structure (`src/`, `docs/`, `public/`, `tests/`)
- Flask backend mit `/events` und `/health` Endpoints
- Erste Frontend‑Seite (`index.html`, `style.css`)
- CI Workflow (`.github/workflows/ci.yml`)
- Dokumentation (`architecture.md`, `user-guide.md`)
